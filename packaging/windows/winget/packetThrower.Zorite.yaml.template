# yaml-language-server: $schema=https://aka.ms/winget-manifest.version.1.12.0.schema.json
#
# Version manifest template. Substitute `${VERSION}` for the
# stable SemVer (e.g. `0.12.0`) when preparing the per-version
# directory under
# `manifests/p/packetThrower/zorite/<version>/`. The `envsubst`
# step in the README handles this.

PackageIdentifier: packetThrower.Zorite
PackageVersion: ${VERSION}
DefaultLocale: en-US
ManifestType: version
ManifestVersion: 1.12.0
